# Contract Template — v1

Human-readable companion fixture for the asset-provenance scenarios. The content is
intentionally minimal: the scenarios assert that attaching this file does not advance the
machine-readable asset's version counter, not that the file is itself parsed.

## Sections

- Scope
- Obligations of the data processor
- Sub-processing
- Termination
